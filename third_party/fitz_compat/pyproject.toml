[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "fitz"
version = "0.0.1.dev2"
description = "Metadata compatibility shim: the fitz import is supplied by PyMuPDF"
requires-python = ">=3.11"
dependencies = ["PyMuPDF>=1.24"]

[tool.setuptools]
py-modules = []
